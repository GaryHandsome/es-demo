1>回调函数：某个函数以参数的方式传递给另一个函数，并在此函数中执行

2>递归函数：自己调用自己的函数

3>闭包：内部函数（内鬼），读取某个函数内部变量的内部函数

休息十分钟

0、课堂练习：自定义对象（分别使用四种方法实现）
属性有：姓名、性别、分数、是否在读、兴趣、地址
方法有：
    showInfo()：打印输出对象的信息
    sum() : 计算N个数的和

要求：
    尽量使用各种数据类型
    规范命名、缩进

    写完，截图发在QQ群上


    package


    BaseServlet - 数据 -> JSON 字符串



1、实现给数组对象扩展一个求和sum方法。

要求：数组中的元素必须是数值型数据，否则抛出异常。

    var arr = [1,2,3,4] ;
    var s = arr.sum() ;
    // 输出:10
    console.log(s) ;

2、定义一个父构造函数Animal，包含的属性和方法有：

● name
● age
● eat()

其中，eat通过原型对象扩展，实现各对象共享此方法。

然后，定义一个子构造函数Cat，包含的属性和方法有：

● sex
● catchMouse()

同时，尝试使用多种方式，实现继承父构造函数Animal。

最后，创建对象，测试。

3、编写程序，使用多态模拟猎人打猎，猎物是一切的动物，具体的动物有如：麻雀、兔子、老虎。当猎人开枪时，各种动物都有逃跑的能力。在程序中，可以通过产生随机数，判断是否打猎成功。

4、编写程序，定义函数sayHello，此函数只可以接收1-3个参数，否则抛出相关的异常。

当接收一个参数时，如张三，则输出你好，张三女士/先生

当接收二个参数时，如张三，中午，则输出中午好，张三女士/先生

当接收三个参数时，如张三，晚上，你吃饭了吗，则输出晚上好，张三女士/先生！您吃饭了吗？

要求：

● 第一个参数为任意姓名；
● 第二个参数只能取值为：早上、中午或晚上中任意一个值；
● 第三个参数可以是任意的提问








